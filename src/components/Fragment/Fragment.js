import React from 'react';

const Fragment = () => {
    return (
        // <> </> is called fragment
        <>
            <h1>My Header</h1>
            <button>Click Me</button>
        </>
    );
};

export default Fragment;